package zornco.megax;


public class MegaXPlayerBase{/* extends PlayerBase
{
	private boolean myModIsActive = true;

	public MegaXPlayerBase(PlayerAPI playerapi)
	{
		super(playerapi);
	}

	// one of my hook functions
	/*public boolean canBreatheUnderwater()
	{
		if(myModIsActive )
		{
			// my code
			System.out.println("air");
			return true;
		}
		else
			return super.canBreatheUnderwater();
	}*/

	// another of my hook functions
	/*public void jump()
    {
            if(myModIsActive)
            {
                    // my code
                    if(callJumpOfEntityPlayerSP)
                            player.localJump();
                    else if(callJumpOfEntityPlayer)
                            player.superJump();
                    // my code
                    if(doGigaJump)
                            player.motionY = gigaJumpSpeed;
                    else if(doMiniJump)
                            player.motionY = miniJumpSpeed;
                    // my code
            }
            else
                    super.jump();
    }*/
}